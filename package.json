{
  "name": "windows-remote-control",
  "version": "1.0.0",
  "description": "Windows Remote Control via Cloudflare Worker + WebSocket",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
